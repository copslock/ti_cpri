#
# This file is the makefile for building UDMA memcpy test app for TI RTOS
#
ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = udma_baremetal_memcpy_testapp
BUILD_OS_TYPE=baremetal

include makefile.mk
